[project]
name = "omoospaceblender"
version = "0.2.6"
description = "Manage your blender projects in omoospace's way"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "omoospace>=0.2.8",
]

[dependency-groups]
dev = [
    "bpy==4.2",
    "pip>=25.3",
    "tomlkit>=0.13.3",
]
