[project]
name = "omoospaceblender"
version = "0.2.4"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "omoospace>=0.2.8",
]

[dependency-groups]
dev = [
    "bpy==4.2",
    "pip>=25.3",
    "tomlkit>=0.13.3",
]
